{% extends "partials/layout.njk" %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/textarea/macro.njk" import govukTextarea %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "partials/createJourneyHeader.njk" import createJourneyHeader %}

{% set pageTitle = "Does anyone need equipment?" %}

{% block content %}
  {{ createJourneyHeader(prisoner, stepsChecked) }}

  <span class="govuk-hint">Book an official visit</span>
  <h1 class="govuk-heading-l">{{ pageTitle }}</h1>
  <p class="govuk-body">Tell us if any visitor needs to bring equipment into the visit, for example a laptop or recording device.</p>

  <form method="post">
    {% for contact in contacts %}
      {% set notesHtml %}
        {{ govukTextarea({
          name: "equipmentNotes-" + contact.contactId,
          id: "equipmentNotes-" + contact.contactId,
          label: { text: "Tell us what equipment is needed" },
          value: contact.equipmentNotes
        }) }}
      {% endset %}

      {{ govukRadios({
        idPrefix: "equipment-" + contact.contactId,
        name: "equipment-" + contact.contactId,
        classes: "govuk-radios--inline",
        fieldset: {
          legend: {
            text: contact.firstName + " " + contact.lastName + " (" + contact.relationshipDescription + ")",
            classes: "govuk-fieldset__legend--m"
          }
        },
        items: [
          { value: "yes", text: "Yes", checked: contact.equipment === true, conditional: { html: notesHtml } },
          { value: "no", text: "No", checked: contact.equipment === false }
        ]
      }) }}
    {% endfor %}

    {{ govukButton({ text: submitText or "Continue" }) }}
  </form>
{% endblock %}
